Phase II:

Fix AssigStat so that it uses VarReference instead of left and index
Fix ArrayAssign so it works
Fix function calls and Procedures. Consider making a FuncCallStat
in addition to the ProcedureStat