let
        var a : int;
        var b, c, d, f : float;

        function print ( n : int ) begin
                printi(n);
        end;
        function twicef ( n : float ) : float begin
            a := 1 + 1 + 1;
            return n + n + 1.0;
        end;
        function ignore ( n : int ) : float begin
            return 5.0;
        end;
in
        f := b + c + d;
        if (1 > 2.3) then
            f := 1 + 2.5 * 3;
        endif;
        a := 1 + 2 + 3;
        b := 1.5 + 1.5 + 2.5 * 3.2 + 1;
        b := twicef(b);
        b := ignore(a);
        print(5);
end